[project]
name = "proxmox-over-ec2"
version = "0.1.0"
description = "Toolchain Python para el paso de Ansible de este lab (ver ansible/README.md)"
requires-python = ">=3.12"
dependencies = [
    "ansible==14.4.0",
]
